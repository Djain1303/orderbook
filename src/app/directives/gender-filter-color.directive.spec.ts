import { GenderFilterColorDirective } from './gender-filter-color.directive';

describe('GenderFilterColorDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderFilterColorDirective();
    expect(directive).toBeTruthy();
  });
});
